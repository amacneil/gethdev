#!/usr/bin/env node
'use strict';

var childProcess = require('child_process');
var path = require('path');

var cmd = 'geth';
var args = ['--dev'];
var procArgs = process.argv.slice(2);
var gethdevJsPath = path.join(path.dirname(__dirname), 'gethdev.js');

if (procArgs.length > 0) {
  // user-specified arguments
  args = args.concat(procArgs);
} else {
  // default arguments
  args.push('js', gethdevJsPath);
}

console.log('>', cmd, args.join(' '));
childProcess.spawnSync('geth', args, { stdio: 'inherit' });
